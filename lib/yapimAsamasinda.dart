import 'package:flutter/material.dart';

class YapimAsamasinda extends StatelessWidget {
  const YapimAsamasinda({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Yapim Aşamasindadır.'),
    );
  }
}
